
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="">
    <meta name="author" content="">
    <link rel="shortcut icon" href="../../docs-assets/ico/favicon.png">

    <title>Feed</title>

    <!-- Bootstrap core CSS -->
    <link href="/css/bootstrap.css" rel="stylesheet">

    <!-- Custom styles for this template -->
    <style>
      body {
        min-height: 2000px;
        padding-top: 20px;
      }
    </style>

    <!-- Just for debugging purposes. Don't actually copy this line! -->
    <!--[if lt IE 9]><script src="../../docs-assets/js/ie8-responsive-file-warning.js"></script><![endif]-->

    <!-- HTML5 shim and Respond.js IE8 support of HTML5 elements and media queries -->
    <!--[if lt IE 9]>
      <script src="https://oss.maxcdn.com/libs/html5shiv/3.7.0/html5shiv.js"></script>
      <script src="https://oss.maxcdn.com/libs/respond.js/1.3.0/respond.min.js"></script>
    <![endif]-->
  </head>

  <body>

    <%
      function getEmail() {
        if(user != null) { return user.email; } 
        else { return "anon"; } 
      }
    %>  

    <div class="container">
      <!-- Static navbar -->
      <div class="navbar navbar-default" role="navigation">
        <div class="navbar-header">
          <a class="navbar-brand" href="#">The Feed</a>
        </div>
        <div class="navbar-collapse collapse">
          <ul class="nav navbar-nav navbar-right">
            <li><a><%= getEmail() %></a></li>
            <li>
              <% if (user != null) { %>
                <a href="./logout">Logout</a>
              <% } else { %>
                <a href="./login">Login</a>
              <% } %>
            </li>
          </ul>
        </div><!--/.nav-collapse -->
      </div>

        <% posts.forEach( function( post ){ %>
      <div class="media">
        <a class="pull-left" href="#">
        <img class="media-object" src="http://placehold.it/60x60">
        </a>
        <div class="media-body">
          <h4 class="media-heading"><%= post.location %></h4>
          <%= post.body %>
        </div>
      </div>
        <% }); %>

    </div> <!-- /container -->
    <br/><br/>
    <div class="container">
      <% if (user != null) { %>
          <h3> Create a post! </h3>

          <form id="postform" action="/post/new" method="post" accept-charset="utf-8">
            <div class="new-post">
            <div>
              <input class="input" size="75" type="text" name="location" placeholder="Where it at? Will use your current location by default..."/>
            </div>
            <br/>
            <div>
              <textarea rows="15" cols="75" name="body" placeholder="Let's hear about this spotted event!"></textarea>
            </div>
            </div>
            <div>
              <button type="submit" class="btn btn-primary" value="Send">Submit</button>
            </div>
          </form>
      <% } %>
    </div> <!-- create a post -->

    <!-- Bootstrap core JavaScript
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->
    <!-- jQuery (necessary for Bootstrap's JavaScript plugins) -->
    <script src="https://code.jquery.com/jquery.js"></script>
    <!-- Include all compiled plugins (below), or include individual files as needed -->
    <script src="/js/bootstrap.js"></script>
  </body>
</html>
